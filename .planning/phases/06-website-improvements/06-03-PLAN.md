---
phase: 06-website-improvements
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - types/index.ts
  - lib/constants.ts
  - components/ui/ServiceCard.tsx
  - components/ui/ServiceCard.module.css
autonomous: true
requirements:
  - IMP-SERVICE-CARDS
  - IMP-FONT-FIX
must_haves:
  truths:
    - "Each of the three service cards shows a full background image with gradient overlay"
    - "Text on service cards is legible — white titles, semi-transparent white body, gold accent subtitle"
    - "Service cards have minimum 320px height to give the image visual presence"
    - "Cards look correct in both light and dark mode (overlay is self-contained, not theme-dependent)"
    - ".cardHighlight uses body font (Inter) with gold accent color instead of decorative Cormorant Garamond italic"
    - "WhatsApp CTA buttons on service cards remain green with white text"
  artifacts:
    - path: "types/index.ts"
      provides: "Updated ServiceItem interface with imageSrc and imageAlt fields"
      contains: "imageSrc"
    - path: "lib/constants.ts"
      provides: "SERVICE_ITEMS with imageSrc and imageAlt values for all 3 services"
      contains: "imageSrc"
    - path: "components/ui/ServiceCard.tsx"
      provides: "ServiceCard with next/image fill background and gradient overlay"
      min_lines: 40
    - path: "components/ui/ServiceCard.module.css"
      provides: "Overlay styles, z-index layering, white/gold text colors on overlay, highlight font fix"
      min_lines: 60
  key_links:
    - from: "components/ui/ServiceCard.tsx"
      to: "types/index.ts ServiceItem.imageSrc"
      via: "service prop destructuring"
      pattern: "service\\.imageSrc"
    - from: "components/ui/ServiceCard.tsx next/image"
      to: "public/images/ files"
      via: "Image fill with src from constants"
      pattern: "src=.*service\\.imageSrc"
    - from: "ServiceCard.module.css .cardOverlay"
      to: "ServiceCard.module.css .cardTitle z-index"
      via: "z-index layering (overlay z:1, content z:2)"
      pattern: "z-index:\\s*[12]"
---

<objective>
Add full background images with gradient overlays to all three service cards, and fix the highlight font from decorative Cormorant Garamond italic to body font (Inter) with gold accent color.

Purpose: Service cards with contextual photography (per IMP-SERVICE-CARDS) create visual impact and communicate the premium positioning. The font highlight fix (per IMP-FONT-FIX) ensures the blockquote text is readable with color-only emphasis.

Output: Updated ServiceItem type, constants with image paths, ServiceCard.tsx with Image fill + overlay, ServiceCard.module.css with z-index layering and highlight fix.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-website-improvements/06-CONTEXT.md
@.planning/phases/06-website-improvements/06-RESEARCH.md
@.planning/phases/06-website-improvements/06-PATTERNS.md

@types/index.ts
@lib/constants.ts
@components/ui/ServiceCard.tsx
@components/ui/ServiceCard.module.css
@components/sections/Services.tsx
@components/sections/Hero.tsx

<interfaces>
<!-- Current ServiceItem interface (types/index.ts lines 1-10): -->
```typescript
export interface ServiceItem {
  id: string
  title: string
  subtitle: string
  description: string
  highlight: string
  items: string[]
  ctaLabel: string
  messageKey: 'fab' | 'eventos' | 'consultoria' | 'curso'
}
```

<!-- Current ServiceCard.tsx usage (lines 16-53): -->
```typescript
export default function ServiceCard({ service }: ServiceCardProps) {
  return (
    <div className={styles.serviceCard}>
      <h3 className={styles.cardTitle}>{service.title}</h3>
      <span className={styles.cardSubtitle}>{service.subtitle}</span>
      <p className={styles.cardDescription}>{service.description}</p>
      <blockquote className={styles.cardHighlight}>{service.highlight}</blockquote>
      <ul className={styles.cardItems}>...</ul>
      <div className={styles.cardCta}><Button .../></div>
    </div>
  )
}
```

<!-- Hero.tsx next/image fill pattern (analog for ServiceCard background): -->
```typescript
<Image
  src="/images/hero-bg.jpeg"
  alt="..."
  fill
  style={{ objectFit: 'cover', objectPosition: 'center top' }}
  sizes="100vw"
  preload={true}
  fetchPriority="high"
  loading="eager"
/>
<div className={styles.heroOverlay} aria-hidden="true" />
```

Available images in public/images/:
- food-table-1.jpeg (elegant dining — Eventos)
- chef-cooking-1.jpeg (professional kitchen — Consultoria)
- students-certificates.jpg (hands-on sushi — Curso)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ServiceItem type and constants with image fields</name>
  <files>types/index.ts, lib/constants.ts</files>
  <action>
**A. Update `types/index.ts` — add imageSrc and imageAlt to ServiceItem:**

Add two fields to the existing `ServiceItem` interface:
```typescript
export interface ServiceItem {
  id: string
  title: string
  subtitle: string
  description: string
  highlight: string
  items: string[]
  ctaLabel: string
  messageKey: 'fab' | 'eventos' | 'consultoria' | 'curso'
  imageSrc: string
  imageAlt: string
}
```

Do NOT modify any other interfaces (GalleryItem, StatItem, NavLink, SocialLink).

**B. Update `lib/constants.ts` — add imageSrc and imageAlt to each SERVICE_ITEMS entry:**

In the `SERVICE_ITEMS` array, add `imageSrc` and `imageAlt` to each of the 3 service objects:

For the **eventos** object (first item), add after `messageKey: 'eventos'`:
```typescript
imageSrc: '/images/food-table-1.jpeg',
imageAlt: 'Mesa elegante com pratos de gastronomia japonesa em evento exclusivo',
```

For the **consultoria** object (second item), add after `messageKey: 'consultoria'`:
```typescript
imageSrc: '/images/chef-cooking-1.jpeg',
imageAlt: 'Chef Cauã Viegas preparando pratos em cozinha profissional',
```

For the **curso** object (third item), add after `messageKey: 'curso'`:
```typescript
imageSrc: '/images/students-certificates.jpg',
imageAlt: 'Alunos com certificados após curso de sushi com o Chef Cauã Viegas',
```

Image assignments per CONTEXT.md Task 6a decisions:
- Eventos Exclusivos -> `food-table-1.jpeg` (elegant plated dining)
- Consultoria -> `chef-cooking-1.jpeg` (professional kitchen/precise prep)
- Curso -> `students-certificates.jpg` (hands-on sushi making)

All alt text in Brazilian Portuguese. Do NOT modify any other constants in this file.
  </action>
  <verify>
    <automated>cd "C:/Users/linck/Desktop/caua viegas sushi" && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>ServiceItem interface has imageSrc and imageAlt fields. All 3 SERVICE_ITEMS entries have real image paths pointing to files in public/images/ with descriptive Portuguese alt text. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add background image + overlay to ServiceCard component and fix highlight font</name>
  <files>components/ui/ServiceCard.tsx, components/ui/ServiceCard.module.css</files>
  <action>
**A. Modify `components/ui/ServiceCard.tsx`:**

Add `import Image from 'next/image'` at the top of imports.

Update the JSX to add the background image layer and overlay. The component remains a Server Component (no "use client" needed — next/image fill works in Server Components). The structure becomes:

```typescript
import type { ServiceItem } from '@/types'
import Image from 'next/image'
import { WHATSAPP_URL_EVENTOS, WHATSAPP_URL_CONSULTORIA, WHATSAPP_URL_CURSO } from '@/lib/constants'
import Button from '@/components/ui/Button'
import styles from './ServiceCard.module.css'

interface ServiceCardProps {
  service: ServiceItem
}

const ctaUrls: Record<string, string> = {
  eventos: WHATSAPP_URL_EVENTOS,
  consultoria: WHATSAPP_URL_CONSULTORIA,
  curso: WHATSAPP_URL_CURSO,
}

export default function ServiceCard({ service }: ServiceCardProps) {
  return (
    <div className={styles.serviceCard}>
      <Image
        src={service.imageSrc}
        alt={service.imageAlt}
        fill
        sizes="(max-width: 767px) 100vw, (max-width: 1023px) 50vw, 33vw"
        style={{ objectFit: 'cover', objectPosition: 'center' }}
        className={styles.cardBgImage}
        loading="lazy"
      />
      <div className={styles.cardOverlay} aria-hidden="true" />
      <h3 className={styles.cardTitle}>{service.title}</h3>
      <span className={styles.cardSubtitle}>{service.subtitle}</span>
      <p className={styles.cardDescription}>{service.description}</p>
      <blockquote className={styles.cardHighlight}>{service.highlight}</blockquote>
      <ul className={styles.cardItems}>
        {service.items.map((item) => (
          <li key={item} className={styles.cardItem}>
            <svg
              className={styles.checkIcon}
              width="10"
              height="10"
              viewBox="0 0 10 10"
              aria-hidden="true"
              fill="currentColor"
            >
              <path d="M8.5 2L4 7.5 1.5 5" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" fill="none" />
            </svg>
            {item}
          </li>
        ))}
      </ul>
      <div className={styles.cardCta}>
        <Button
          href={ctaUrls[service.id]}
          target="_blank"
          rel="noopener noreferrer"
          variant="whatsapp"
          aria-label={`${service.ctaLabel} — ${service.title}`}
        >
          {service.ctaLabel}
        </Button>
      </div>
    </div>
  )
}
```

Key points:
- `<Image>` with `fill` is the FIRST child — renders as absolute-positioned background (z-index 0 via CSS)
- `<div className={styles.cardOverlay}>` is the SECOND child — overlay gradient (z-index 1 via CSS)
- All other children (title, subtitle, description, highlight, items, CTA) get `position: relative; z-index: 2` via CSS
- `loading="lazy"` because service cards are below the fold
- No `"use client"` added — this stays a Server Component

**B. Rewrite `components/ui/ServiceCard.module.css`:**

Replace the entire file:

```css
.serviceCard {
  display: flex;
  flex-direction: column;
  position: relative;
  min-height: 360px;
  overflow: hidden;
  background-color: transparent;
  border: none;
  border-radius: var(--radius-card, 16px);
  padding: 32px;
  transition: transform 300ms ease-in-out,
              box-shadow 300ms ease-in-out;
}

.serviceCard:hover {
  transform: scale(1.03);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
}

/* Background image — absolute fill, z-index 0 */
.cardBgImage {
  z-index: 0;
}

/* Two-layer overlay: dark vignette + bottom gradient for text legibility
   Per CONTEXT.md: rgba(0,0,0,0.45) overall + bottom gradient rgba(0,0,0,0.72) at 0% to rgba(0,0,0,0.15) at 55% */
.cardOverlay {
  position: absolute;
  inset: 0;
  z-index: 1;
  background:
    linear-gradient(
      to top,
      rgba(0, 0, 0, 0.72) 0%,
      rgba(0, 0, 0, 0.35) 45%,
      rgba(0, 0, 0, 0.45) 100%
    );
  border-radius: var(--radius-card, 16px);
}

/* All text/content elements: position relative + z-index 2 to sit above overlay */
.cardTitle {
  position: relative;
  z-index: 2;
  font-family: var(--font-display, Georgia, serif);
  font-size: 24px;
  font-weight: 600;
  line-height: 1.2;
  color: #FFFFFF;
}

.cardSubtitle {
  position: relative;
  z-index: 2;
  display: block;
  font-size: 13px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-accent);
  margin-top: 12px;
}

.cardDescription {
  position: relative;
  z-index: 2;
  font-size: 15px;
  font-weight: 400;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.85);
  margin-top: 24px;
}

/* Highlight font fix (IMP-FONT-FIX):
   - Removed: font-family: var(--font-display) (was Cormorant Garamond)
   - Removed: font-style: italic
   - Changed: color from muted gray to gold accent for emphasis
   - Kept: border-left gold accent bar */
.cardHighlight {
  position: relative;
  z-index: 2;
  font-size: 15px;
  font-style: normal;
  line-height: 1.6;
  color: var(--color-accent);
  border-left: 2px solid var(--color-accent);
  padding-left: 16px;
  margin: 24px 0 0 0;
}

.cardItems {
  position: relative;
  z-index: 2;
  list-style: none;
  padding: 0;
  margin: 24px 0 0 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.cardItem {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 14px;
  color: rgba(255, 255, 255, 0.85);
}

.checkIcon {
  flex-shrink: 0;
  width: 12px;
  height: 12px;
  color: var(--color-accent);
}

.cardCta {
  position: relative;
  z-index: 2;
  margin-top: auto;
  padding-top: 28px;
}

@media (prefers-reduced-motion: reduce) {
  .serviceCard {
    transition: none;
  }
  .serviceCard:hover {
    transform: none;
  }
}
```

Key changes from Phase 5 version:
1. `.serviceCard`: Added `position: relative`, `min-height: 360px`, `overflow: hidden`, changed `background-color` to `transparent`, removed `border`
2. `.cardBgImage`: New class for the background image z-index
3. `.cardOverlay`: New absolute overlay with two-layer gradient (dark vignette + bottom emphasis)
4. All text elements: Added `position: relative; z-index: 2`, changed colors to white/semi-transparent white for legibility on dark overlay
5. `.cardHighlight`: **Font fix** — removed `font-family: var(--font-display)`, removed `font-style: italic`, changed color to `var(--color-accent)` (gold). Font inherits body font (Inter) via cascade. The `border-left` gold accent bar is preserved.
6. `.cardSubtitle` and `.checkIcon`: Use `var(--color-accent)` which stays gold in both light and dark mode
7. Cards are self-contained — the overlay defines all visual contrast, so they look identical in light and dark mode

Per CONTEXT.md: "CTA button: retain green with white text (WhatsApp color)" — Button's `.whatsapp` class already handles this, no override needed.
  </action>
  <verify>
    <automated>cd "C:/Users/linck/Desktop/caua viegas sushi" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>ServiceCard shows full background image with gradient overlay. Text is white/gold on overlay. .cardHighlight uses body font (Inter) with gold accent color. Cards are self-contained and work in both light and dark mode. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Image src from constants | Image paths are from lib/constants.ts — trusted internal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | I (Information Disclosure) | Service card images | accept | Images are intentionally public marketing photos — no sensitive content |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `pnpm build` — build succeeds
3. ServiceCard renders with background image visible through gradient overlay
4. Text on cards is legible (white titles, gold subtitle, semi-transparent white body)
5. `.cardHighlight` uses body font (not Cormorant Garamond), gold color, no italic
6. Cards look correct at mobile, tablet, and desktop breakpoints
</verification>

<success_criteria>
- All 3 service cards have background images (food-table-1.jpeg, chef-cooking-1.jpeg, students-certificates.jpg)
- Gradient overlay makes text legible on all images
- .cardHighlight uses Inter font with gold accent color (not decorative serif italic)
- ServiceItem type has imageSrc and imageAlt fields
- Cards render identically in light and dark mode
- `pnpm build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-website-improvements/06-03-SUMMARY.md`
</output>
